# FBSavePro
Cloudflare Pages starter project.

Structure: `public/` for the website and `functions/api/video.js` for `/api/video`.

Pages settings: Framework None, Build command `exit 0`, Build output directory `public`, Root `/`.

The API only validates Facebook URLs and does not bypass private/protected content.
