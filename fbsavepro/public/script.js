const form=document.querySelector('#form'),input=document.querySelector('#url'),status=document.querySelector('#status'),result=document.querySelector('#result');document.querySelector('#year').textContent=new Date().getFullYear();document.querySelector('#paste').onclick=async()=>{try{input.value=await navigator.clipboard.readText();status.textContent='URL pasted. Select Download.'}catch{status.textContent='Clipboard unavailable; paste the URL manually.'}};function valid(v){try{const u=new URL(v);return ['facebook.com','m.facebook.com','web.facebook.com','fb.watch'].includes(u.hostname.toLowerCase().replace(/^www\./,''))}catch{return false}}form.onsubmit=async e=>{e.preventDefault();result.hidden=true;status.textContent='';if(!valid(input.value.trim())){status.textContent='Please enter a valid Facebook URL.';return}const btn=form.querySelector('button[type=submit]');btn.disabled=true;btn.textContent='Checking…';status.textContent='Processing URL…';try{const r=await fetch('/api/video',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url:input.value.trim()})});const d=await r.json();if(!r.ok)throw Error(d.error||'Request failed');result.hidden=false;result.innerHTML=`<h3>${d.title||'URL validated'}</h3><p>${d.message||'No download is configured yet.'}</p>`+(d.downloadUrl?`<a href="${d.downloadUrl}" target="_blank" rel="noopener">Download Video</a>`:'')}catch(err){status.textContent=err.message}finally{btn.disabled=false;btn.textContent='Download'}};
